name: generate

# 仅在主源或脚本/工作流自身改动时触发,避免 v2rayn-rules.json 改动再次触发(死循环)
on:
  pull_request:
    paths:
      - "proxy-list.txt"
      - "proxy-ip-list.txt"
      - "proxy-ip-auto.txt"
      - "generate.py"
      - "classify_safe_domain_pr.py"
      - "fetch_telegram_ips.py"
      - "tests/**"
      - ".github/workflows/**"
  push:
    branches:
      - main
    paths:
      - "proxy-list.txt"
      - "proxy-ip-list.txt"
      - "generate.py"
      - "classify_safe_domain_pr.py"
      - "fetch_telegram_ips.py"
      - "tests/**"
      - ".github/workflows/**"
  workflow_dispatch:

# 两个会写回仓库的工作流共用同一把锁,避免同时推送。
concurrency:
  group: proxy-rules-writer
  cancel-in-progress: false

# PR 中运行的代码只有只读权限；仅可信 main 的非 PR 任务可以写回派生产物。
permissions:
  contents: read

jobs:
  generate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Install test dependency
        run: python -m pip install pytest

      - name: Generate rule files
        run: python generate.py

      - name: Run full test suite
        run: python -m pytest tests/ -q

      - name: Verify generated files are committed
        run: git diff --exit-code -- v2rayn-rules.json shadowrocket.conf shadowrocket.module

  publish:
    if: github.event_name != 'pull_request'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Install test dependency
        run: python -m pip install pytest

      - name: Generate rule files
        run: python generate.py

      - name: Run full test suite
        run: python -m pytest tests/ -q

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add v2rayn-rules.json shadowrocket.conf shadowrocket.module
          if git diff --cached --quiet; then
            echo "生成结果无变化,跳过提交"
          else
            git commit -m "auto: 重新生成规则文件(v2rayn-rules.json / shadowrocket.conf / shadowrocket.module)"
            git push
          fi
