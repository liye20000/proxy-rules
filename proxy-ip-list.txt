# 代理 IP 段白名单 - 手动主源文件
# 每行一个 CIDR(IPv4 或 IPv6),以 # 开头的行为注释;支持行内注释。
# 用于「按 IP 段走代理」的服务(连接不经域名/DNS、直连 IP 的场景)。
# 修改后 GitHub Actions 会自动重新生成派生文件。
#
# 注意:Telegram 的 IP 段由 fetch_telegram_ips.py 每天自动抓取,写在
#       proxy-ip-auto.txt(切勿手动编辑那个文件)。本文件只放你手动维护的额外 IP 段。
#       生成时两份会合并去重。
#
# 最后更新:2026-06-12

# (目前没有手动维护的 IP 段;需要时按下面格式添加)
# # ===== 某服务 =====
# 203.0.113.0/24
